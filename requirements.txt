# Canonical requirements — ONLY what the pipeline actually imports.
# Resolves cleanly on Python 3.11–3.13. Install with:
#
#   python3.13 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# Versions are floors (>=) so pip picks whatever is valid for your Python — that's
# what avoids the "no matching distribution" errors you hit with exact pins.
#
# The full frozen environment (heavy/unused: onnxruntime, torch, chromadb, docling,
# nipype, sentence-transformers, …) is archived in requirements-full.txt for
# reference only; the pipeline does NOT need it (RAG uses FAISS + scikit-learn).

# ── Orchestration + LLM (default provider: groq) ──────────────────────────────
langgraph>=1.0
langchain-core>=1.0
langchain-groq>=1.0
# Uncomment the provider you set in LLM_PROVIDER (each is imported lazily):
# langchain-google-genai>=4.0      # LLM_PROVIDER=gemini
# langchain-openai>=1.0            # LLM_PROVIDER=openai
# langchain-ollama>=1.0            # LLM_PROVIDER=ollama

# ── RAG (TF-IDF + SVD over FAISS — NOT chromadb/torch) ────────────────────────
faiss-cpu>=1.8
scikit-learn>=1.4
numpy>=1.26

# ── Document text extraction + OCR ────────────────────────────────────────────
pdfplumber>=0.11
PyMuPDF>=1.24
pdfminer.six>=20231228
pytesseract>=0.3.13
Pillow>=10.0

# ── Backend (FastAPI) ─────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
pydantic>=2.7

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0
tenacity>=8.2
colorlog>=6.8

# ── Gmail watcher (Part 2 — only needed if you use the real Gmail trigger) ────
google-api-python-client>=2.150
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# ── Legacy Streamlit UI only (skip if you use the React/FastAPI UI) ───────────
# streamlit>=1.40
# pandas>=2.2
